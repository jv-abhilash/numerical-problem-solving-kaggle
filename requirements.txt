python-dotenv>=1.0
pandas>=2.0,<2.3            # Kaggle ships 2.2.x; safe pin to avoid bigframes conflicts
huggingface_hub>=0.24
transformers>=4.41
accelerate>=0.30
einops>=0.7
sentencepiece>=0.1.99
bitsandbytes>=0.43 ; platform_system == "Linux" and platform_machine == "x86_64"

# Only if you use datasets / GCS / BigQuery:
datasets>=3.0
fsspec==2025.3.2
gcsfs==2025.3.2
google-cloud-bigquery>=3.31.0

google-cloud-bigquery-storage>=2.30.0,<3.0.0


rich<14
